# Workspace repository

- Use `E:\2026数模\private` as the default repository for subsequent tasks in this workspace unless the user specifies otherwise.
- Repository origin: `https://github.com/hyym-wq/private.git`.
- Run repository commands from that directory.
