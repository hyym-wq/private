# Q3：Q2模型的首次达标时间

沿用用户指定的一、二问框架，以第二问附录3变参数模型演化整个过程。目标 t*=inf{t: max_r C(r,t)<0.15}。继承一维固定半径、有效Robin传质边界和4小时后环境保持末值的假设。无需新增拟合参数或优化算法。

已有第二问计算为可复用基准；主计算重跑并检查全网格事件，径向40段仅作数值诊断。中心是否控制停止由计算检查，不预先作为唯一停止条件。严格不等式在连续时间下报告阈值穿越时刻与首次离散达标时刻的区别。

主要风险：网格误差、末环境延拓、最多3次迭代未必每步收敛、输出舍入不能用于达标判断。第三问不增加潜热或收缩（收缩由第四问处理）。
