fonts 目录说明
==============
本目录用于存放论文编译所需的内嵌字体（如需要嵌入 PDF 的中文字体文件，
例如 simsun.ttc、simhei.ttf 等）。默认情况下 ctex 宏包会调用系统已安装的
中文字体，通常无需在本目录放置任何文件即可正常编译。

如遇字体缺失或乱码，请将对应字体文件复制到本目录，并在 format.cls 中
通过 \setCJKmainfont 等命令指定。
