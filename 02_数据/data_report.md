# 第一问输入核对

输入来源为工作区官方 A 题 PDF、附件 1 和附件 3 的 result1.xlsx，完整路径及 SHA-256 记录在 data_profile.json。原文件未改动。

附件 1 的 Sheet1 含 241 条数值记录，时间从 0 到 14400 s，以 60 s 等间隔排列，三列分别为时间、温度、水分浓度。三个字段无缺失、无非数值/无穷值，时间键及整行均无重复。没有剔除、插补或平滑数据。清洗仅将字段改为 time_s、T_inf_degC、C_eq_kg_per_kg 并转为 UTF-8 BOM CSV。

第一问使用 0 至 1800 s 共 31 条记录并作分段线性插值，不需要外推。初始环境为 28 °C、0.01963 kg/kg，1800 s 环境为 41.513 °C、0.03307 kg/kg。

result1.xlsx 模板工作表名为“温度”“水分浓度”，A1 为“时间\到药材中心的距离”。模板省略号必须展开，最终 B1:V1 为 0:0.1:2 cm，A2:A1801 为 1:1:1800 s；每表 1800×21 个计算值，按 0.0000 格式显示。t=0 初值保存于求解证据中，不增添至官方模板结果行。论文另需两个 7×5 表。

数据可用于第一问，需披露三项假设：环境 kg/kg 在等效传质边界内直接作为 C_eq；二维径向输出位于中截面 z=0；端面系数等于侧面。无内部温湿实测值，模型验证仅涉及数学/数值正确性与模型内差异，不能写成实验预测误差。

详细字段统计、单位、哈希和风险见同目录 data_profile.json。清洗文件为“清洗数据/0910_第一问_烘房边界_v01_Codex.csv”。这是确定性外部边界，不将 241 个采样点当作相互独立的拟合样本。

