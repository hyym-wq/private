# 第一问论文写作依据

用户明确要求：“根据这个内容写一个论文用latex编译，只写第一问，写法参考mathmodeling skill里面的内容”。本目录据此形成第一问独立论文稿，不推断为外部提交或用户完成科学结论验收。

## 权威内容

- 方法和论证范围：`04_结果/第一问/0910_第一问_结果与模型说明_v01_Codex.md`。
- 结果表：`04_结果/第一问/q1_export.json`，主解为 `q1_main_r16`。
- 求解配置与过程残差：`04_结果/第一问/q1_main_r16_summary.json`。
- 中截面数值差和平均量对照：`04_结果/第一问/experiments/round1/metrics/q1_convergence.json`。
- 独立温度参照：`03_代码/第一问/reviews/q1_independent_checks.json`。
- 用户确认：`methods/Q1/q1_decisions.jsonl` 中的 `q1_midplane_output_confirmation`，仅确认中截面输出及附对照、说明、验证。

## 写作范围与证据映射

正文：问题分析与假设 → PDE 与初边值条件 → 守恒有限体积与非线性 CN → 指定结果及剖面 → 数值验证与适用范围。

`writing_snapshot.json` 为脚本生成的写作快照，记录引用值、JSON 定位及源文件哈希。未创建需要独立人工签收的 `frozen_numbers.json`，未伪造 `package_signoff`。参考论文写作与润色技能的叙事、公式、数字溯源、结论校准和图表规范；不以本稿代替整场比赛提交审计。

| 正文内容 | 证据 | 呈现及限制 |
|---|---|---|
| 二维 z=0 径向输出 | 用户口径确认、方法说明 | 不扩大到任意轴向位置 |
| 两张7×5数值表 | q1_export 两张 table 数组 | 四位显示，逐单元对照完整导出矩阵 |
| 表面梯度随时间变化 | 主解 snapshots_t/c 的 z 索引0 | 正式结果矢量图，从原精度数据重绘 |
| 端面仍影响体积均值 | convergence 的 final_*_means | 结构差异说明，不宣称全域四位精度 |
| 中截面计算收敛 | convergence 的4组 max_abs | 网格差不是严格误差上界 |
| 温度解析参照 | independent 中256/512项及主解误差 | 径向参照，配合中截面对照使用 |
| 整体守恒 | 主运行 summary 中两个 balance 指标 | 容量归一化残差，不是质量kg或相对误差 |

正式图件计划：图1 `figures/q1_radial_profiles.pdf`，类型3结果图，由主解未舍入剖面生成；左温度、右干基含水率，r单位cm、七个指定时刻、同一图例。内容限于既有结果说明中已作出的分布解释，不添加创新性或物理实验精度结论。

## 校准后的措辞

- 第一问两场分别求解，不写强耦合。
- CN在新层内迭代更新扩散率，不写全步冻结旧D。
- 时间步为预设分级启动，不写误差驱动自适应。
- Ceq=C∞是经验闭合；潜热等机制未引入，不说已证明可忽略。
- 图表数字保留题设位数；数值收敛不替代物理验证。
- 不提出没有对照证据支持的“首创、最优、显著优于”等主张。
