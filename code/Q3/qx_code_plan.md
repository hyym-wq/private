# 第三问计算约定

用户授权：在一、二问现有模型基础上解答第三问（q3_inherit_q2）。继承第二问 p2_model.py 的公式、环境插值、初值、边界和数值格式，不将第一问常参数解拼接到第二问初期。

主结果直接复用第二问求解器，N=20、dt=1 s、最多3次迭代。基准为已完成的第二问同参数运行，其中心首次低于0.15的时间为202240 s；新计算独立检查每一秒全部节点的最大值。N=40、dt=1 s仅作网格诊断，不替换正式结果，不引入第二套物理方法。

输入：只读官方附件1（s、摄氏度、kg/kg），模板附件3/result3.xlsx。4 h后沿用末环境值，此为继承的延拓假设。运行最长72 h；若仍未达标则显式失败。

停止判据：max_j C_j < 0.15，记录首次达标秒数及前一步最大含水率，辅助线性插值定位阈值。检查径向单调性、非负、有限值。必须用未四舍五入值判定。

输出：results/Q3/result3.xlsx，Sheet1保持模板名称，时间60 s起每60 s，0..2 cm每0.1 cm；若终点不是60 s倍数，额外附终点行。表5每6 h及最终时间，四位小数。NPZ保留未舍入采样值；JSON保留阈值、环境、源文件摘要、运行耗时和诊断。

验证：与第二问已算202240 s对照、N=40网格对照、Excel回读核验。不把有限网格阈值定位误差视作物理模型误差，不宣称四位小数均已收敛。不修改前两问文件。

## 基于已算证据的数值加密

N20得到56.1777778 h，N40得到56.7055556 h，差0.5277778 h。晚期阈值时间对空间误差敏感，故追加N80、N160（dt仍为1 s），物理模型及边界不变。以N160生成更细网格的第三问结果，在题目要求的0.1 cm位置直接抽样；保留N20口径文件供前两问对照。此为计算精度调整，不是用户重新选择物理方法，也不意味着结果冻结。
