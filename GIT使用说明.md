# Git 使用说明（比赛期间够用版）

Git 用来保存文件修改记录，并通过远程仓库交换三人的工作。`commit` 是保存在本机的一次记录；`push` 后队友才能从远程获得这些记录。

## 首次使用前

团队共用一个远程仓库和一个 `main` 分支，每人在自己的电脑上保留一份项目。远程仓库和首次连接由团队一起确认后配置；其他成员通过克隆获取项目。仅执行 `git init` 不会创建 GitHub 仓库，也不会使 `pull` / `push` 自动可用。

下面的日常流程以远程仓库、身份和 `main` 跟踪关系已经准备好为前提。在项目文件夹内打开终端执行命令。不要在不同日期重复初始化仓库。

## 开始工作

确认上次工作已经保存并提交，再运行：

```sh
git pull
```

含义：从远程仓库获取队友的新提交，并合并到本地。成功后再开始修改；如果提示本地改动未处理，先停止，保留改动并检查原因。

修改前沟通自己要改哪些文件。同一个文件尽量不要两个人同时修改，尤其是论文主文件和公共函数。

## 完成一小部分

先保存文件，确认代码或论文检查通过，再逐条执行：

```sh
git status
git add .
git commit -m "说明本次完成的内容"
git push
```

- `git status`：查看哪些文件新增、修改或删除。先确认列表没有误删、秘密信息或不应上传的大文件。
- `git add .`：把当前目录及子目录中未被忽略的改动加入“暂存区”，作为下次提交的候选内容；它也会暂存删除操作，不只是新文件。
- `git commit -m "说明本次完成的内容"`：把暂存区内容保存成一次本地修改记录。将引号内文字换成具体说明，例如“第一问：完成缺失值处理”。
- `git push`：把本地提交上传到已连接的远程仓库，让队友可以拉取。

每完成一个可说明的小任务就提交一次。提交不等于已共享，推送成功才表示远程收到提交。

## 出现冲突或报错

如果 `git pull` / `git push` 出现冲突或报错，先停止操作，保留文件和报错信息，联系队友或请有经验的人检查问题后再处理。

- 不要自行强制覆盖，不要使用 `git push --force`。
- 不要随便执行 `git reset --hard`，它可能丢失尚未提交的改动。
- 不要为消除报错而删除项目、`.git` 或队友文件。
- 推送被拒绝可能是队友已经先推送；这不代表必须覆盖远程。先检查，再决定如何同步。

比赛期间不需要学习或使用复杂的 branch / rebase / cherry-pick 工作流。
